<%- include('../Layout/Header') %>
    <link rel="stylesheet" href="//cdn.datatables.net/1.12.1/css/jquery.dataTables.min.css">
    <%- include('../Layout/Sidebar') %>
        <style>
            /* Set display to none for image*/
        </style>
        <div id="main">
            <div class="page-heading">
                <div class="page-title mb-3">
                    <div class="row">
                        <div class="col-12 col-md-6 order-md-1 order-last">
                            <h3><a href="<%= baseUrl %>dashboard" class="text-primary"><i
                                        class="fas fa-arrow-left me-2"></i></a> Donators</h3>
                        </div>
                        <div class="col-12 col-md-6 order-md-2 order-first">
                            <nav aria-label="breadcrumb" class="breadcrumb-header float-start float-lg-end">
                                <ol class="breadcrumb">
                                    <li class="breadcrumb-item"><a href="<%= baseUrl %>dashboard">Dashboard</a></li>

                                </ol>
                            </nav>
                        </div>
                    </div>
                </div>
                <section class="section">
                    <div class="card">

                        <div class="category-filter">
                            <label>
                                Country: 
                                <select id="categoryFilter" class="form-control">
                                  <option value="">Show All</option>
                                  <% var i=1; country1.forEach(function(v) { %>
                                    <option value="<%= v._id.country %>"><%= v._id.country %></option>
                                    <% }) %>
                                </select>        
                            </label>        
                          </div>

                        <div class="card-body">
                            <div class="dataTable-container">
                                <div class="success" style="display: none;"></div>
                                <table class="table table-striped dataTable-table" id="filterTable">
                                    <thead>
                                        <tr>
                                        <th>No.</th>
                                        <th>Member Id</th>
                                        <th>UserName</th>
                                        <th>TG-id</th>
                                        <th>Email</th>
                                        <th>Country</th>
                                        <th>JoinDate</th>
                                        <th>Action</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% var i=1; donateduser.forEach(function(v) { %>
                                            <% if( v.usertype == 2) { %>
                                            <tr data-id="<%= v._id%>">
                                                <td><span class="name-field">
                                                    <%= i %>
                                                </span></td>
                                                <td><span class="name-field">
                                                        <%= v.memberid%>
                                                    </span></td>
                                                <td><span class="name-field">
                                                        <%= v.username%>
                                                    </span></td>
                                                <td><span class="name-field">
                                                        <a href="https://t.me/<%= v.tgid%>"> https://t.me/<%= v.tgid%></a>
                                                    </span></td>
                                                <td><span class="name-field">
                                                        <a href='mailto:<%= v.email%>'>
                                                            <%= v.email%>
                                                        </a>
                                                    </span></td>
                                                <td><span class="name-field">
                                                        <%= v.country%>
                                                    </span></td>
                                                <td><span class="name-field">
                                                    <%= moment(v.date).format('DD-MM-YYYY') %>
                                                    </span></td>
                                                <td>
                                                    <button type="button" data-id="<%= v._id %>" id="refresh"
                                                        class="btn icon icon-left btn-danger btn-sm delete-data"><i
                                                            class="bi bi-trash"></i> </button>
                                                </td>
                                            </tr>
                                            <% i++ %>
                                        <% }%>
                                            <% }) %>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
            </div>
        </div>

        </section>
        <footer>
            <div class="footer clearfix mb-0 text-muted">
                <div class="float-start">
                    <p>2023 &copy; All rights are reserved</p>
                </div>
                
            </div>
        </footer>
        </div>

        <div class="modal fade bd-example-modal-lg" id="viewQRCode" tabindex="-1" role="dialog"
            aria-labelledby="myLargeModalLabel" aria-hidden="true">
            <div class="modal-dialog modal-sm modal-dialog-centered">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title w-100" id="exampleModalLabel">
                            QRcode Preview
                        </h5>
                        <button type="button" class="close" data-bs-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">×</span>
                        </button>
                    </div>
                    <!--Modal body with image-->
                    <div class="modal-body">
                        <img src="" class="imagepreview" id="image" width="100%" height="265px">
                    </div>
                    <!-- <div class="modal-footer">
                        <button type="button" class="btn btn-danger"
                            data-bs-dismiss="modal" >
                            Close
                        </button>
                    </div> -->
                </div>
            </div>
        </div>
        <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
        <script src="//cdn.datatables.net/1.12.1/js/jquery.dataTables.min.js"></script>

        <script>
            //   $(document).ready(function() {
            //     var table = $('#table1').DataTable({
            //       "bSort": false
            //     });
            //   });
            $("document").ready(function () {
                  $("#filterTable").dataTable({
                    "searching": true
                  });
                  var table = $('#filterTable').DataTable();
                  $("#filterTable_filter.dataTables_filter").prepend($(".category-filter"));
                  
                  var categoryIndex = 5;
                //   $("#filterTable th").each(function (i) {
                //     if ($($(this)).html() == "Country") {
                //       categoryIndex = i; return false;
                //     }
                //   });
                  $.fn.dataTable.ext.search.push(
                    function (settings, data, dataIndex) {
                      var selectedItem = $('#categoryFilter').val()
                      var category = data[categoryIndex];
                      console.log(selectedItem);
                      if (selectedItem === "" || category.includes(selectedItem)) {
                        return true;
                      }
                      return false;
                    }
                  );
                  $("#categoryFilter").change(function (e) {
                    table.draw();
                  });
                  table.draw();
                });
            </script>

        <script>
            $(document).ready(function () {
                var table = $('#table1').DataTable( {
                        "bSort": false
                    } );
            });
            $(".btnID").on("click", function () {                
                var images = $(this).find('.image').prop('src');

                $('#viewQRCode #image').prop('src', images);
                $('#viewQRCode').modal('show');
            });
        </script>
<script>
    $(document).on('click', '.delete-data', function () {
        var id = $(this).data("id");
        var baseUrl = '<%= baseUrl %>'
        swal({
            title: "Are you sure?",
            text: "You want to delete this User?",
            icon: "warning",
            buttons: true,
            dangerMode: true,
        })
            .then((willDelete) => {
                if (willDelete) {
                    $.ajax({
                        type: "GET",
                        url: `${baseUrl}admin/deletedonateduser?id=${id}`,
                        dataType: "json",
                        statusCode: {
                            200: function (xhr) {
                                dataResult = xhr;
                                $(`tr[data-id="${id}"]`).remove();
                                console.log(dataResult.message)
                                Toastify({
                                    text: "Deleted successfully...",
                                    duration: 3000,
                                    newWindow: true,
                                    close: true,
                                    gravity: "top", // `top` or `bottom`
                                    position: "right", // `left`, `center` or `right`
                                    stopOnFocus: true, // Prevents dismissing of toast on hover
                                    style: {
                                        background: "#ff2c2c",
                                    },
                                }).showToast();
                            }
                        },
                    });
                }
            });
    });
    let isTost = '<%= session.isTost %>'
    let msg = '<%= session.tostMsg %>'
    let tostBackground = '<%= session.tostBackground %>'
    if (isTost == "true") {
        Toastify({
            text: msg,
            duration: 3000,
            newWindow: true,
            close: true,
            gravity: "top", // `top` or `bottom`
            position: "right", // `left`, `center` or `right`
            stopOnFocus: true, // Prevents dismissing of toast on hover
            style: {
                background: tostBackground,
            },
        }).showToast();
        '<%= session.isTost = "false"%>'
    }
</script>


        <%- include('../Layout/Footer') %>